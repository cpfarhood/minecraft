apiVersion: k8s.mariadb.com/v1alpha1
kind: Grant
metadata:
  name: pterodactyl
spec:
  mariaDbRef:
    name: pterodactyl-db
  privileges:
    - ALL
  database: main
  table: "*"
  username: pterodactyl
  host: "%"
